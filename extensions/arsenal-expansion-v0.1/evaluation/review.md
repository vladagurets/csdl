# Arsenal Expansion Review

## Contract review

- Eight recipe candidates have distinct nearest-neighbor statements.
- Three component candidates have repeated semantic uses.
- A/B/C remain unchanged; Editorial, Structural, and Analytical are modes.
- Six analytical datasets are synthetic, typed, fixed, and deterministic.
- The sixty-file accepted raster inventory remains protected.

## Visual review state

Three independent candidates exist for every recipe. All twenty-four active
candidates are `1920x1080` PNG files in RGB or RGBA mode, have unique SHA-256
hashes, and preserve the approved visible copy. On 2026-09-03 the user selected
`v1` for all eight recipes. The eight package-local selected files are
byte-identical to their recalibrated sources and remain publicly unpromoted.
Raster metadata, source hashes, selected hashes, and exact-copy status are
recorded in `selection/candidate-hashes.yaml`.

On 2026-09-03 the user rejected the complete visual pass as too subdued and
compositionally uninteresting. The existing twenty-four files remain preserved
as rejected evidence. A direction-specific recalibration started with
`024 Causal Chain`; the user approved its stronger visual ambition and the same
standard was then applied to recipes `025-031`. The replacement pass now
contains twenty-four candidates with materially stronger visual mechanisms and
composition topologies. No candidate is promoted before explicit per-recipe
selection.

### 024 Causal Chain

- Pass status: rejected as part of the complete first visual pass.
- `v1.png`: quiet horizontal chain with a narrow coral verification hinge.
- `v2.png`: diagonal source-to-test-to-release chain with one coral test edge.
- `v3.png`: compact gated relay with a coral release destination.
- Rejected evidence: the first `v2` used several competing coral blocks. It is
  preserved as `rejected-v2-pass1.png`; the active `v2` is the regenerated pass.

#### Direction-specific recalibration

- Expression: Level B, Structural.
- `v1` direction: causal impact. A large diagonal route collides with an
  oversized coral test barrier and visibly deflects the defect away from the
  protected release route. Topology: full-canvas diagonal sweep with one
  off-center collision.
- `v2` direction: evidence reveal. A monumental inspection aperture exposes a
  coral deviation seam inside a dense code mass and passes only a clean route.
  Topology: two unequal Fields divided by one central vertical mechanism.
- `v3` direction: protected release. A strong boundary encloses the release
  Field while a dark defect mass breaks against the outside at one coral hinge.
  Topology: compressed cause zone against a dominant protected region.
- Candidate-divergence review: pass. The concepts, mechanisms, and composition
  topologies are materially different; none is a mirror, palette swap, or
  spacing variant of another.
- Exact-copy review: pass by full-resolution visual inspection. Each candidate
  renders the four approved strings exactly once and adds no text.
- Raster review: pass. All normalized candidates are unique `1920x1080` RGB
  PNGs. Sources are `1672x941` RGB and were resized mechanically without crop,
  redraw, recolor, or text edit.
- Board: `selection/recalibration/024-causal-chain.png`.
- Hash evidence: `selection/recalibration/candidate-hashes.yaml`.
- Selection status: `v1` selected and accepted at `4.86`; public promotion not
  executed.

### 025 State Machine

- `v1.png`: horizontal primary route with a lower REWORK feedback path.
- `v2.png`: compact vertical state network with READY as the terminal state.
- `v3.png`: open diagonal branch with a compact REWORK return.
- Rejected evidence: the first `v1` made both REWORK and READY dominant coral
  states. It is preserved as `rejected-v1-pass1.png`; the active `v1` is the
  regenerated pass.

#### Direction-specific recalibration

- Expression: Level B, Structural.
- `v1` direction: guarded orbit. DRAFT, REVIEW, READY, and REWORK form a wide
  operational loop around a coral review gate. Topology: open circular system
  with the title as an upper-left counterweight.
- `v2` direction: review pressure chamber. REVIEW becomes the dominant central
  state, with DRAFT entering from the left, READY released upward, and REWORK
  returning below. Topology: dense central mechanism with orthogonal pressure
  routes.
- `v3` direction: folded execution route. A thick angular path rises from DRAFT
  through REVIEW to READY while REWORK creates a deep coral return fold.
  Topology: monumental zigzag spanning the full width.
- Rejected evidence: the first recalibrated `v3` duplicated `REVIEW`; it is
  preserved as `rejected-v3-pass1.png`. The active `v3` is the exact-copy
  regeneration.
- Candidate-divergence review: pass. Loop, chamber, and folded-route concepts
  use different mechanisms and topologies.
- Exact-copy review: pass by full-resolution visual inspection.
- Raster review: pass. Three unique `1920x1080` RGB PNGs, mechanically resized
  from `1672x941` RGB sources without crop, recolor, redraw, or text edit.
- Board: `selection/recalibration/025-state-machine.png`.
- Selection status: `v1` selected and accepted at `4.71`; public promotion not
  executed.

### 026 Dependency Map

- `v1.png`: API-centered dependency fan with an explicit unstable feedback path.
- `v2.png`: left-to-right dependency map with UI and TESTS converging on SHIP.
- `v3.png`: open topology with API instability interrupting the release routes.
- Rejection reason: none after full-size visual review.

#### Direction-specific recalibration

- Expression: Level B, Structural.
- `v1` direction: load-bearing bridge. UI and TESTS span between SPEC and SHIP;
  API is the visibly cracked support. Topology: full-width bridge with a single
  failure point below the deck.
- `v2` direction: dependency gravity. SHIP hangs above a suspended dependency
  assembly whose unstable API pulls the system downward. Topology: centered
  hanging mass with strong vertical force.
- `v3` direction: critical chokepoint. SPEC, UI, and TESTS compress through one
  coral API throat before entering the large SHIP field. Topology: asymmetric
  funnel into a dominant destination.
- Candidate-divergence review: pass. Bridge, suspended mass, and chokepoint are
  materially different representations.
- Exact-copy review: pass by full-resolution visual inspection.
- Raster review: pass. Three unique `1920x1080` RGB PNGs, mechanically resized
  from `1672x941` RGB sources.
- Board: `selection/recalibration/026-dependency-map.png`.
- Selection status: `v1` selected and accepted at `4.86`; public promotion not
  executed.

### 027 Claim / Evidence

- `v1.png`: three evidence items converge on a compact claim and confirmation.
- `v2.png`: claim-first proof line with evidence suspended beneath it.
- `v3.png`: editorial split field with the claim and confirmation opposite the
  evidence spine.
- Rejection reason: none after full-size visual review.

#### Direction-specific recalibration

- Expression: Level B, Structural.
- `v1` direction: proof vault. The claim is a monumental slab carried by three
  evidence supports and locked by a coral verdict. Topology: heavy upper mass
  over a visible structural base.
- `v2` direction: evidence beam. Three evidence pylons support a long claim
  cantilever that terminates in confirmation. Topology: diagonal load path with
  a single coral terminal support.
- `v3` direction: argument aperture. Three evidence channels pass through a
  narrow opening, condense into the claim, and release into confirmation.
  Topology: opposing fields divided by one compression mechanism.
- Candidate-divergence review: pass. Vault, cantilever, and aperture differ in
  concept, mechanism, and spatial organization.
- Exact-copy review: pass by full-resolution visual inspection.
- Raster review: pass. Three unique `1920x1080` RGB PNGs, mechanically resized
  from `1672x941` RGB sources.
- Board: `selection/recalibration/027-claim-evidence.png`.
- Selection status: `v1` selected and accepted at `4.86`; public promotion not
  executed.

### 028 Spectrum

- `v1.png`: continuous axis with a coral review threshold before AGENT.
- `v2.png`: rising capability field divided by a single review threshold.
- `v3.png`: quiet modular axis with one coral ASSISTANT threshold.
- Rejected evidence: the first `v3` introduced prohibited random dot fields. It
  is preserved as `rejected-v3-pass1.png`; the active `v3` is the regenerated
  pass.

#### Direction-specific recalibration

- Expression: Level B, Structural.
- `v1` direction: autonomy terrain. A rigid SCRIPT platform grows into a broad
  sloped autonomy field, with a coral inspection boundary before AGENT.
  Topology: panoramic diagonal landscape.
- `v2` direction: tension arc. SCRIPT, ASSISTANT, and AGENT sit on one enormous
  rising structural arc; a coral clamp marks the review requirement near the
  high-autonomy end. Topology: centered monumental curve.
- `v3` direction: freedom expansion. A narrow SCRIPT channel unfolds through
  ASSISTANT into a large AGENT aperture bounded by a coral perimeter. Topology:
  extreme left-to-right expansion.
- Candidate-divergence review: pass. Terrain, tension arc, and expanding
  aperture are visibly independent mechanisms.
- Exact-copy review: pass by full-resolution visual inspection.
- Raster review: pass. Three unique `1920x1080` RGB PNGs, mechanically resized
  from `1672x941` RGB sources.
- Board: `selection/recalibration/028-spectrum.png`.
- Selection status: `v1` selected and accepted at `4.57`; public promotion not
  executed.

### 029 Anatomy

- `v1.png`: open offset assembly of five task parts around a strong title anchor.
- `v2.png`: bounded modular anatomy with CHECKS as the coral internal signal.
- `v3.png`: interlocking part field with CHECKS joining four surrounding regions.
- Rejection reason: none after full-size visual review.

#### Direction-specific recalibration

- Expression: Level B, Structural.
- `v1` direction: interlocking core. Five differently shaped parts assemble
  into one task body, with a coral seam at the CHECKS/EVIDENCE fit. Topology:
  one large off-center object.
- `v2` direction: functional cross-section. The five parts become chambers
  inside one thick cutaway vessel, with EVIDENCE exiting through a coral
  aperture. Topology: horizontal cropped section.
- `v3` direction: reliability tension. BRIEF and EVIDENCE are opposing supports
  carrying a span formed by CONSTRAINTS, TOOLS, and CHECKS. Topology: dramatic
  bridge-like diagonal over a large void.
- Candidate-divergence review: pass. Assembly, cutaway, and tension structure
  do not collapse into the same composition.
- Exact-copy review: pass by full-resolution visual inspection.
- Raster review: pass. Three unique `1920x1080` RGB PNGs, mechanically resized
  from `1672x941` RGB sources.
- Board: `selection/recalibration/029-anatomy.png`.
- Selection status: `v1` selected and accepted at `4.86`; public promotion not
  executed.

### 030 Layer Stack

- `v1.png`: literal vertical stack with an execution direction marker.
- `v2.png`: sequential layer axis from POLICY through DATA.
- `v3.png`: nested open brackets that expose dependency depth without cards.
- Rejection reason: none after full-size visual review.

#### Direction-specific recalibration

- Expression: Level B, Structural.
- `v1` direction: tectonic strata. Four offset layers carry different visual
  mass while one coral fault transmits pressure through the stack. Topology:
  low, wide, frame-cropped strata.
- `v2` direction: responsibility spine. POLICY descends through a coral conduit
  into ORCHESTRATION, TOOLS, and a broad DATA foundation. Topology: tall
  off-center load-bearing spine.
- `v3` direction: execution cascade. Four massive planes descend from POLICY to
  DATA through coral spillways. Topology: sweeping upper-left to lower-right
  cascade with extreme scale change.
- Candidate-divergence review: pass. Strata, spine, and cascade create three
  distinct readings of layered execution.
- Exact-copy review: pass by full-resolution visual inspection.
- Raster review: pass. Three unique `1920x1080` RGB PNGs, mechanically resized
  from `1672x941` RGB sources.
- Board: `selection/recalibration/030-layer-stack.png`.
- Selection status: `v1` selected and accepted at `4.86`; public promotion not
  executed.

### 031 Roadmap

- `v1.png`: three-lane temporal roadmap with a diagonal progression trace.
- `v2.png`: NOW/NEXT/LATER matrix with one guarded cross-lane dependency.
- `v3.png`: stepped route from BASELINE through evidence to guarded AUTONOMY.
- Rejected evidence: the first `v2` duplicated milestone copy and added `TRACE`.
  The first `v3` added `TRACE`, then its next pass introduced prohibited dot
  fields. The rejected first passes are preserved as `rejected-v2-pass1.png`
  and `rejected-v3-pass1.png`; the active candidates are regenerated passes.

#### Direction-specific recalibration

- Expression: Level B, Structural.
- `v1` direction: braided workstreams. CONTRACT, BASELINE, EVALS, GUARDS,
  MEMORY, and AUTONOMY flow as three interlaced paths across NOW, NEXT, and
  LATER. Topology: sweeping braid from lower-left to upper-right.
- `v2` direction: expanding reliability corridor. The roadmap widens through
  three structural gates, from the compressed NOW entrance into the open LATER
  field. Topology: deep horizontal corridor with unequal temporal zones.
- `v3` direction: guarded ascent. NOW, NEXT, and LATER become three unequal
  platforms connected by one coral guardrail. Topology: monumental rising
  silhouette across the lower two-thirds.
- Candidate-divergence review: pass. Braid, corridor, and ascent use separate
  representations and composition logic.
- Exact-copy review: pass by full-resolution visual inspection.
- Raster review: pass. Three unique `1920x1080` RGB PNGs, mechanically resized
  from `1672x941` RGB sources.
- Board: `selection/recalibration/031-roadmap.png`.
- Selection status: `v1` selected and accepted at `4.86`; public promotion not
  executed.

## Selection gate

Selection is complete: `024:v1, 025:v1, 026:v1, 027:v1, 028:v1, 029:v1,
030:v1, 031:v1`. Exact-copy review, rubric scoring, byte-identity checks, and
candidate-divergence review pass for all eight choices. The package status is
`selected_unpromoted`.

Public promotion remains a separate gate. Component Library v0.1, Recipe
Library v0.5, Analytical Mode v0.1, Prompt DSL v0.5, and the sixty accepted
baseline raster hashes remain unchanged. Promotion still requires an explicit
vocabulary decision for Threshold, Trace, Band, and the five candidate
relations, followed by separately versioned target packages.

Active overview: `selection/overview.png`.
Selection record: `selection/selected.yaml`.
Combined hash evidence: `selection/candidate-hashes.yaml`.
Accepted scores: `evaluation/scores.csv`.
