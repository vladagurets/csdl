id: '025'
slug: state-machine
name: State Machine
version: 0.6-candidate
status: candidate
problem: Explain discrete states, guarded transitions, and terminal or recurrent behavior.
distinguishes_from: Decision Tree branches once; State Machine permits guarded transitions and return paths.
allowed_scenarios: [task lifecycle, agent control state, approval state]
ingredients:
  required:
    - {component: Node, min: 3, max: 6, default: 4}
    - {component: Vector, min: 2, max: 7, default: 4}
    - {component: Threshold, min: 1, max: 3, default: 1}
    - {component: Signal, min: 1, max: 1, default: 1}
  optional:
    - {component: Trace, min: 0, max: 1, default: 1}
    - {component: Loop, min: 0, max: 1, default: 0}
relations:
  allowed:
    - {subject: Node, type: transitions_to, object: Node}
    - {subject: Vector, type: directs, object: Node}
    - {subject: Vector, type: crosses, object: Threshold}
    - {subject: Trace, type: orders, object: Node}
  forbidden:
    - {subject: Node, type: causes, object: Node}
assembly_order: [declare states, declare transitions, attach conditions, distinguish current or terminal state]
expression_levels: {A: allowed, B: allowed, C: forbidden}
expression_modes: [structural]
content_contract: {required: [headline, states, transition_conditions], exact: true, max_words: 42}
prompt: prompts/025-state-machine.yaml
specification: recipes/025-state-machine.md
record: recipes/025-state-machine.yaml
