# Migration

The baseline remains valid. A future promotion copies only accepted candidate
records into new versioned libraries, updates compatibility indexes, and bumps
the Prompt DSL package version if the public component or relation enum changes.
Existing v0.5 packages remain readable without normalization.
