# Rollback

Remove the additive `extensions/arsenal-expansion-v0.1/` package and its two
tools/tests. No baseline contract or accepted raster needs restoration because
the extension does not edit them.
